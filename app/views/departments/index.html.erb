<div class="jumbotron text-center">
  <h1>Welcome to the Department Store</h1>
  <p>Select a Department to View Items</p>
</div>

<div class = container>
  <ul style="list-style:none;">
    <% @departments.each do |d| %>
      <li><%= link_to d.name, department_path(d) %></li>
    <% end %>
  </ul>
  <br/>
  <button><%= link_to 'Create a New Department', new_department_path %></button>
</div>