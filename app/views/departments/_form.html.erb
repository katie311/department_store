<h1><%= @department.id ? 'Edit Department' : 'Create Department' %></h1>
<hr />
<%= form_for @department do |d| %>
  <%= d.label :name %>
  <%= d.text_field :name, required: true, autofocus: true %>
  <%= d.submit %>
<% end %>
<%= link_to "Back", departments_path %>